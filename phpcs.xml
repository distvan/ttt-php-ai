<?xml version="1.0"?>
<ruleset name="CustomStandard">
    <description>My custom PHPCS standard</description>

    <!-- Extend PSR12 -->
    <rule ref="PSR12" />

    <!-- Ignore specific folders -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
</ruleset>
